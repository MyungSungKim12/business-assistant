[project]
name = "business-assistant-desktop"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
  "business-assistant-common",
  "PySide6>=6.8,<7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
business-assistant-common = { workspace = true }
